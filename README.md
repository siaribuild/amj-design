
  > **⚠️ DEPRECATED — do not develop against this repo.**
  >
  > This is a stale, pre-rebrand snapshot (last updated 2026-07-16, frontend
  > only — no `worker/` backend). All active development, including the
  > OpenFrame rebrand, moved to **[siaribuild/apertly](https://github.com/siaribuild/apertly)**.
  > That repo is what's deployed at openframe.com.au.
  >
  > This repo is scheduled for deletion. If you're looking for the current
  > codebase, use `apertly` instead.

  # AMJ Trade Direct Website

  This is a code bundle for AMJ Trade Direct Website. The original project is available at https://www.figma.com/design/faLQcDOCh1z4pLTIqOfuWb/AMJ-Trade-Direct-Website.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  