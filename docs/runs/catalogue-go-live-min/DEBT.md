- [low] Verify Finding 1 (pre-existing, not this feature): two cycle-cap tests in scripts/tests/pipeline.test.mjs fail at the base commit d8f994aa too — 'a cycle-cap refusal stops next cleanly' and 'a third verify/fix cycle is refused' expect /CYCLE CAP/ and get 'stage "verify" did not write docs/runs/demo/06-verify.md'. Belongs to the conductor's own suite; reproduce with npm run test:pure.
- [low] Ponytail review (07-review-ponytail.md): ~61 lines of dead code and redundant tests — stable() never called; NEW_GLAZINGS/buildSeo/rekey/same/normProfile exported with no importer; families/categories fetched and folded into ids but never looked up; mutate() returns a discarded value; altered() has one call site; five tests that read literals back from P or duplicate assertSafe checks. Harvest with ponytail-debt after the production write, not before — the fix round rewrites the same functions.
- [low] Ponytail round 2 (07-review-ponytail.md after fix round 1): the same class as round 1, now ~70 lines — stable(), unused exports, P built with an add() wrapper, specRow/keySpecs duplication, isDG regex, plannedOptionsBySlug computed for all products, redundant and literal-readback tests, stale line-number comments. Harvest with ponytail-debt after the production write.
- [medium] Architecture review round 2, "verification can self-certify a wrong planner": several tests read P (the sheet table) and assert its own values, and the convergence fixture replays the planner's own mutations. Restructure the critical assertions (frame system, hardware, profile, disabled) to go through plan()'s mutation output and a post-state built from it, and stop exporting helpers only so tests can inspect them. Not blocking go-live: the tester's round-2 evidence for those criteria is mutation-based.
- [low] Ponytail round 3 (07-review-ponytail.md over fc97a962): 23 items, ~80 lines possible, same class as rounds 1–2 (dead helpers, one-caller wrappers, literal-readback tests, stale comments). Harvest with ponytail-debt after the production write.
- [medium] Architecture round 3, finding 1: an EXISTING new-profile document is rewritten with createOrReplace from the planner's partial model (no _rev precondition; unmodelled fields such as orderRank are dropped). Harmless for the first production write — the six profiles do not exist yet and a converged plan emits no replace — but a later re-run after a Studio edit would overwrite it. Fix: patch existing profiles field-by-field with ifRevisionID (as products are), reserving createOrReplace for documents that do not exist.
